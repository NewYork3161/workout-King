<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000"
    tools:context=".LoginScreen">

    <!-- Username / Email -->
    <EditText
        android:id="@+id/etLoginUsername"
        android:layout_width="0dp"
        android:layout_height="56dp"
        android:hint="Username or Email"
        android:background="@drawable/input_blue_outline"
        android:padding="12dp"
        android:textColor="#000000"
        android:textColorHint="#888888"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="56dp"
        android:layout_marginHorizontal="32dp"/>

    <!-- Password (WITH BUILT-IN EYE TOGGLE) -->
    <com.google.android.material.textfield.TextInputLayout
        android:id="@+id/tilLoginPassword"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:layout_marginHorizontal="32dp"
        app:passwordToggleEnabled="true"
        app:passwordToggleTint="#00B7FF"
        app:layout_constraintTop_toBottomOf="@id/etLoginUsername"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <EditText
            android:id="@+id/etLoginPassword"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:hint="Password"
            android:inputType="textPassword"
            android:background="@drawable/input_blue_outline"
            android:padding="12dp"
            android:textColor="#000000"
            android:textColorHint="#888888"/>
    </com.google.android.material.textfield.TextInputLayout>

    <!-- Create Account Text -->
    <TextView
        android:id="@+id/tvCreateAccount"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Create an account."
        android:textColor="#00B7FF"
        android:textSize="16sp"
        app:layout_constraintTop_toBottomOf="@id/tilLoginPassword"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginTop="32dp"
        android:layout_marginStart="32dp"/>

    <!-- Already Have Account Text -->
    <TextView
        android:id="@+id/tvAlreadyAccount"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Already have an account?"
        android:textColor="#00B7FF"
        android:textSize="16sp"
        app:layout_constraintTop_toBottomOf="@id/tilLoginPassword"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="32dp"
        android:layout_marginEnd="32dp"/>

    <!-- Create Account Continue (LEFT) -->
    <ImageView
        android:id="@+id/imgCreateAccountContinue"
        android:layout_width="129dp"
        android:layout_height="70dp"
        android:layout_marginTop="12dp"
        android:src="@drawable/continue_btn"
        android:contentDescription="Create Account Continue"
        android:clickable="true"
        android:focusable="true"
        app:layout_constraintTop_toBottomOf="@id/tvCreateAccount"
        app:layout_constraintStart_toStartOf="@id/tvCreateAccount"/>

    <!-- Login Continue (RIGHT) -->
    <ImageView
        android:id="@+id/imgLoginContinue"
        android:layout_width="129dp"
        android:layout_height="70dp"
        android:layout_marginTop="12dp"
        android:src="@drawable/login_btn"
        android:contentDescription="Login Continue"
        android:clickable="true"
        android:focusable="true"
        app:layout_constraintTop_toBottomOf="@id/tvAlreadyAccount"
        app:layout_constraintStart_toStartOf="@id/tvAlreadyAccount"/>

    <!-- Logo Bottom Right -->
    <ImageView
        android:id="@+id/imgLogo"
        android:layout_width="310dp"
        android:layout_height="165dp"
        android:layout_margin="24dp"
        android:layout_marginBottom="16dp"
        android:src="@drawable/wk_logo"
        android:contentDescription="Workout King Logo"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>
